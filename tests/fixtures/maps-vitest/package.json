{
  "name": "maps-vitest-fixture",
  "version": "1.0.0",
  "scripts": {
    "test": "vitest run",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "vitest": "^4.0.0",
    "playwright": "^1.0.0"
  }
}
