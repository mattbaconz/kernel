{
  "name": "map-fixture",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "release": "npm publish",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  }
}
